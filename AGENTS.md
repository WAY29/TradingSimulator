# 项目准则

## 前端设计

- 好的前端应通过布局、控件和即时状态反馈自己说人话，不用说明性文案解释界面。
- 遵循奥卡姆剃刀和 Less is more：没有实际行为的按钮不渲染，不为模仿其他产品而添加摆设。
- 一个功能只保留一个入口；优先复用 KLineChart Pro 已有的画线和指标能力。
- 重复出现或共享语义的 UI 必须复用同一组件或渲染函数及同一套样式，禁止复制 HTML/CSS 分别维护；修改共享字段、交互或视觉时，必须枚举并验证草稿、待成交、已成交、Live 和 Replay 等所有适用状态，避免只改一部分。
- 页面默认展示当前市场；历史回放只有在用户开启 Replay 后才接管图表。
- 行情状态必须来自 TradingView 实时推送；价格变化时立即更新标题、图表和自选表，禁止用固定轮询伪装实时行情。
- 修改 UI 后必须用真实浏览器检查桌面和窄屏布局、交互状态与数据更新。
- 市场和交易所图标必须使用 TradingView 返回或其官方 symbol/provider logo，不用手工字符或自造颜色冒充；无法取得时才显示中性文字回退。

## 产品边界

- 本项目只做历史回放和模拟交易，不接入真实下单。
- 回放模式只向图表提供当前回放点之前的数据，不能提前加载未来 K 线。
